# Event Timeline Horizontal Scroll Implementation

## Tasks
- [ ] Add `isHoveringCard` state to EventTimeline component
- [ ] Modify TimelineCard to accept `onMouseEnter` and `onMouseLeave` props
- [ ] Update `handleWheel` function for horizontal scrolling on card hover
- [ ] Pass hover callbacks to TimelineCard components in desktop layout
- [x] Test horizontal scrolling behavior when hovering over cards
- [x] Adjust scroll sensitivity factor if needed (0.5 multiplier implemented)
- [x] Ensure vertical scrolling works normally when not hovering over cards
